export enum Navigation {
  LoginButton = 'div.ut-login-content > button',
  BackButton = "[class='ut-navigation-button-control']",
  Home = '.icon-home',
  Squads = '.icon-squad',
  Sbc = '.icon-sbc',
  Transfers = '.icon-transfer',
  Stadium = '.icon-stadium',
  Store = '.icon-store',
  Club = '.icon-club',
  Leaderboards = '.icon-leaderboards',
  Settings = '.icon-settings',
}

export enum AutoFut {
  jsonMessageElementId = 'autofut-json-message',
}
