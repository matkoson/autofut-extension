/**
 * The general strategy for selectors is to perform the query, and possibly filtering of results, if feasible.
 * The signature will indicate what possible output is, and the caller will need to react accordingly.
 */
export * from './transfers'
export * from './shared'
export * from './navigation'
export * from './club'
